@model Item
<li class="item">
    <p class="title"><a href="@Model.Link">@Model.Title</a></p>
    <p class="meta">
        @Model.Score points | <a href="@Model.CommentsLink">@Model.Comments comments</a>
    </p>
</li>